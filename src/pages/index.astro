---
import Layout from "../layouts/Layout.astro";
import { useCloudState } from "freestyle-sh";
import Counter, { CloudCounter } from "../components/Counter.svelte";

const count = await useCloudState(CloudCounter).getCount();
---

<Layout title="Freestyle Astro">
  <main>
    <div>
      <h1>freestyle.sh – Astro</h1>
      <Counter client:load count={count} />
    </div>
  </main>
</Layout>

<style>
  body {
    margin: 0;
    padding: 0;
    background-color: rgb(37, 37, 44);
  }

  main {
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: sans-serif;
    color: white;
    margin-top: 2rem;
  }

  div {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }
</style>
